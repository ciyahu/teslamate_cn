# 官方 v4.2.0 到当前分支的更新说明

本文只记录当前分支相对 TeslaMate 官方 `v4.2.0` 的差异，用于以后上游继续更新时快速判断哪些改动需要继续保留。

当前分支：

- 分支：`ciyahu/sync-v4.2.0-trial`
- 当前提交：`a94ba679`
- 上游基准：TeslaMate 官方 `v4.2.0`
- 测试镜像：`ciyahu/teslamate2:latest`
- 正式镜像：`ciyahu/teslamate:latest`

## 合并方式

本次不是直接复制文件覆盖，而是在已有中国本地化改动基础上合并官方 `v4.2.0`：

```bash
git merge --no-ff v4.2.0
```

冲突只出现在官方 GHCR 相关 workflow：

- `.github/workflows/ghcr_build.yml`
- `.github/workflows/ghcr_purge.yml`

当前 fork 不使用官方 GHCR 发布流程，而是使用自己的 Docker Hub workflow，所以这两个文件继续保持删除。

## Grafana 目录保护规则

`grafana/` 整个目录使用本项目 `old-main` 的版本，后续同步 TeslaMate 上游时不处理、不合并上游对该目录的任何改动。

该规则覆盖 `grafana/` 下的全部内容，包括：

- Grafana `Dockerfile` 和基础镜像版本。
- Dashboard JSON 及其 UID、跳转链接和中文本地化。
- datasource、dashboard provisioning 配置。
- Grafana 相关脚本、图标和静态资源。

执行上游合并时，如 `grafana/` 出现新增、删除、修改或冲突，统一保留本项目合并前的完整目录，不接受上游版本。需要升级 Grafana 或调整 Dashboard 时，必须作为独立任务单独评估、修改和测试，不能夹带在 TeslaMate 上游同步中。

## Actions 和构建流程

相对官方 `v4.2.0`，当前分支保留自定义 GitHub Actions。

### 删除的官方 workflow

当前分支删除了官方拆分式 CI/CD workflow，例如：

- `buildx.yml`
- `devops.yml`
- `elixir_test.yml`
- `ensure_linting.yml`
- `elixir_dep_verification_and_static_analysis.yml`
- `osv-scanner.yml`
- `spell_check.yml`
- `stale.yml`
- `cleanup_caches.yml`
- `cleanup_largest_caches.yml`
- `check_paths.yml`
- `check_if_pr_from_outside_repo.yml`
- `ghcr_build.yml`
- `ghcr_purge.yml`
- `update-flake-lock.yml`

原因：

- 这些是官方项目自己的 PR、GHCR、依赖扫描、拼写检查、缓存清理流程。
- 当前 fork 的目标是构建自己的 Docker Hub 镜像，不需要全部保留官方发布链路。
- 保留它们会增加维护成本，也可能在 fork 中因为 secret、权限或仓库策略不同而失败。

### 新增/保留的自定义 workflow

当前分支保留：

- `.github/workflows/test.yml`
- `.github/workflows/main.yml`
- `.github/workflows/grafana.yml`

逻辑：

- `test.yml` 是集中式 Code Check。
  - 编译无 warning。
  - 跑测试。
  - 构建前端 assets。
  - 校验 Grafana dashboard JSON。
  - 已增加 `ciyahu/**` 分支触发，方便当前试验分支自动/手动检查。
- `main.yml` 是 Docker Hub 多架构构建上传。
  - 构建 `linux/amd64`。
  - 构建 `linux/arm64`。
  - 按 digest 上传。
  - 最后合并 manifest。
  - 当前测试地址是 `ciyahu/teslamate2:latest`。
- `grafana.yml` 用于 Grafana 相关校验/流程，保留当前 fork 的简化维护方式。

正式发布规则：

- 测试阶段先推 `ciyahu/teslamate2:latest`。
- 测试确认后，把 `.github/workflows/main.yml` 的 `DOCKER_REPO` 从 `ciyahu/teslamate2` 改回 `ciyahu/teslamate`，重新提交并跑正式构建。

## 中国本地化地图逻辑

当前分支相对官方 `v4.2.0` 保留了地图本地化。

相关文件：

- `assets/js/hooks.js`
- `assets/css/app.scss`
- `assets/css/themes/dark.scss`
- `lib/teslamate/settings/global_settings.ex`
- `lib/teslamate_web/live/settings_live/index.ex`
- `lib/teslamate_web/live/settings_live/index.html.heex`
- `lib/teslamate_web/templates/layout/root.html.heex`
- `test/teslamate_web/live/settings_test.exs`

### 设置页地图选择

官方没有腾讯/高德地图选择。

当前分支使用设置项 `tencent_map_enabled` 存储地图选择：

- `true`：腾讯地图
- `false`：高德地图

这样做的原因：

- 复用之前已经存在的腾讯地图开关状态。
- 不新增数据库迁移。
- 避免用户升级时多一列新字段。
- 旧的腾讯 key 字段继续留在数据库里，但当前前端和后端不再读写它。

### 腾讯地图 key

当前分支去除了腾讯地图 key 的输入和读写逻辑。

实际行为：

- 设置页不显示腾讯 key 输入框。
- 后端不再读取/保存腾讯 key。
- 腾讯地图固定使用空白 key。
- 腾讯地图 SDK 的鉴权失败浮层在页面上隐藏。
- 腾讯地图鉴权提示兼容新旧两种文案：
  - 旧文案：`鉴权失败，请传入正确的key`
  - 新文案：`当前产品鉴权失败，请检查KEY及相关信息是否准确。`

注意：

- 隐藏鉴权失败提示不等于腾讯官方鉴权成功。
- 这是为了当前使用方式下页面不被错误浮层遮挡。

### 高德地图

当前分支新增/修复了高德地图支持：

- 设置页下拉可选择高德地图。
- 高德地图可以拖动。
- 高德地图支持滚轮缩放。
- 高德地图隐藏鉴权失败提示。
- 高德 marker 使用 `priv/static/images/car.png`。
- 高德 marker 的朝向按腾讯地图同一逻辑对齐，避免车头方向相反。

### marker 图片

新增文件：

- `priv/static/images/car.png`

逻辑：

- 腾讯地图和高德地图都使用同一张车图。
- 不再使用高德默认蓝色圆点。
- 方向由车辆 heading 计算后应用到 marker。

## 地图全屏和销毁清理

当前分支保留地图全屏按钮和销毁清理逻辑。

逻辑说明：

- 全屏按钮用于在地图控件内切换更大的地图显示区域。
- 销毁清理是指 LiveView 页面切换、地图 DOM 被移除、hook 被重新挂载时，主动释放地图实例、事件监听器和状态引用。
- 这样可以减少重复初始化、事件重复绑定、旧地图对象残留导致的异常。

相关文件：

- `assets/js/hooks.js`
- `assets/css/app.scss`
- `assets/css/themes/dark.scss`

## 地理编码和地址服务

当前分支把官方默认地理编码/HTTP 请求逻辑改为自定义地址服务。

相关文件：

- `lib/teslamate/http.ex`
- `lib/teslamate/locations/geocoder.ex`
- `test/teslamate/http_test.exs`
- `test/teslamate/locations/geocoder_test.exs`

逻辑：

- 地址服务固定使用 `https://address.wemate.top`。
- 保留官方新版 `Tesla.client(...)` 相关写法时，目标地址仍指向自定义服务。
- 这样能兼容官方 4.2.0 的 HTTP 客户端结构，同时继续走中国可用的地址服务。

## 车辆状态扩展字段

当前分支在 Tesla API vehicle state 中保留扩展字段。

相关文件：

- `lib/tesla_api/vehicle/state.ex`

扩展字段：

- `charge_port_color`
- `driver_assist`
- `now_playing_title`

用途：

- 保留更多车辆状态信息。
- 供后续页面、统计或外部集成使用。

## 车辆摘要和费用页面本地化

当前分支相对官方保留部分页面本地化/显示调整。

相关文件：

- `lib/teslamate/vehicles/vehicle/summary.ex`
- `lib/teslamate_web/live/car_live/summary.html.heex`
- `lib/teslamate_web/live/charge_live/cost.html.heex`
- `lib/teslamate_web/views/layout_view.ex`
- `test/teslamate_web/live/car_summary_live_test.exs`

逻辑：

- 保留面向当前使用习惯的车辆摘要显示。
- 保留费用页面相关显示调整。
- 测试也同步修改，避免官方默认文案/默认逻辑与当前 fork 行为不一致。

## Tencent map 数据库迁移

当前分支相对官方新增：

- `priv/repo/migrations/20260615120000_add_tencent_map_settings.exs`

逻辑：

- 这是历史上为腾讯地图设置增加字段的迁移。
- 当前分支虽然不再读取/写入腾讯 key，但保留该迁移。
- 原因是已有用户数据库可能已经执行过该迁移，新用户从空库安装也需要迁移链完整。
- 不再新增新的地图 provider 字段，避免继续扩张数据库结构。

## README 和样式

当前分支相对官方保留：

- `README.md` 的 fork 说明/使用说明。
- `assets/css/app.scss` 和 `assets/css/themes/dark.scss` 的地图相关样式。

样式主要服务于：

- 地图全屏按钮。
- 高德/腾讯地图容器。
- marker 或地图控件显示。
- 隐藏地图 SDK 鉴权失败浮层。

## 验证记录

### 本地验证

已执行：

```bash
git diff --check
```

结果：

- 没有空白错误。
- Windows 下提示 workflow 文件 LF/CRLF 转换，不影响提交和 CI。

已执行 Grafana JSON 校验：

```bash
node -e "..."
```

结果：

- `Grafana dashboard JSON OK`

本地限制：

- 本机没有 `mix` 命令，所以完整 Elixir 编译测试依赖 GitHub Actions。
- 本机没有 `docker` 命令，所以镜像 manifest 的最终 inspect 依赖 GitHub Actions。

### GitHub Code Check

- run：`32676318704`
- 地址：`https://github.com/ciyahu/teslamate_cn/actions/runs/32676318704`
- 提交：`2242d7fc`
- 结果：成功

覆盖内容：

- `mix compile --warnings-as-errors`
- `mix test --warnings-as-errors`
- `npm --prefix assets ci`
- `npm --prefix assets run deploy`
- Grafana dashboard JSON 校验

### Docker 测试镜像

- run：`32676497113`
- 地址：`https://github.com/ciyahu/teslamate_cn/actions/runs/32676497113`
- 提交：`2242d7fc`
- 镜像：`ciyahu/teslamate2:latest`
- 结果：成功

覆盖平台：

- `linux/amd64`
- `linux/arm64`

最终结果：

- 两个平台 build by digest 成功。
- digest artifact 上传成功。
- manifest list 创建成功。
- workflow 内部 `docker buildx imagetools inspect ciyahu/teslamate2:latest` 成功。

### Docker 正式镜像

- run：`32677581139`
- 地址：`https://github.com/ciyahu/teslamate_cn/actions/runs/32677581139`
- 提交：`4ec96946`
- 镜像：`ciyahu/teslamate:latest`
- 结果：成功

覆盖平台：

- `linux/amd64`
- `linux/arm64`

最终结果：

- 两个平台 build by digest 成功。
- digest artifact 上传成功。
- manifest list 创建成功。
- workflow 内部 `docker buildx imagetools inspect ciyahu/teslamate:latest` 成功。

### 腾讯地图鉴权提示更新

- 修改文件：`lib/teslamate_web/templates/layout/root.html.heex`
- 原因：腾讯地图 SDK 的鉴权失败提示从旧文案变成了新文案。
- 处理：隐藏逻辑从单个字符串匹配改为新旧文案数组匹配。
- 当前覆盖：
  - `鉴权失败，请传入正确的key`
  - `当前产品鉴权失败，请检查KEY及相关信息是否准确。`

验证结果：

- 提交：`a94ba679`
- Code Check：
  - run：`33346167976`
  - 结果：成功
- Docker 正式镜像：
  - run：`33346172504`
  - 地址：`https://github.com/ciyahu/teslamate_cn/actions/runs/33346172504`
  - 镜像：`ciyahu/teslamate:latest`
  - 结果：成功
  - 覆盖平台：`linux/amd64`、`linux/arm64`
  - manifest 创建和 workflow 内部 `Inspect image` 成功

## 后续上游更新时怎么处理

下次官方更新时，优先在新分支上做：

```bash
git fetch upstream --tags
git switch -c ciyahu/sync-vX.Y.Z-trial
git merge --no-ff vX.Y.Z
```

重点检查：

- 不处理 `grafana/` 目录；无论上游如何变更，该目录都完整保留本项目版本。
- 官方是否修改 `assets/js/hooks.js` 中地图 hook 周边逻辑。
- 官方是否修改 settings/global settings 存储结构。
- 官方是否修改 Tesla HTTP client 或 geocoder 调用方式。
- 官方是否修改 vehicle state 结构。
- 官方是否修改 LiveView 生命周期，影响地图销毁清理。
- 官方是否修改 Dockerfile、mix.lock、assets 构建方式。
- 官方是否新增必须保留的安全/数据库迁移。

合并原则：

- `grafana/` 目录始终排除在上游同步范围之外。
- 官方 bugfix 和结构性更新尽量保留。
- 中国地图、地址服务、车辆扩展字段、自定义 Docker workflow 继续按当前 fork 逻辑重新套上去。
- 先推测试镜像 `ciyahu/teslamate2:latest`。
- 测试确认后再推正式镜像 `ciyahu/teslamate:latest`。
